# Locode Roadmap — v0.2 → v0.5

**Date:** 2026-03-10
**Status:** Proposed

---

## Version Progression

Each version builds on the previous. No version can be skipped.

```
v0.1 (current)          v0.2                    v0.3                    v0.3.5                  v0.4                    v0.5
─────────────────────── ─────────────────────── ─────────────────────── ─────────────────────── ─────────────────────── ───────────────────────
Routing CLI             Tool Foundation         Coding Agent            Hardening + Performance  Codebase Intelligence   Workflow Automation
                        │                       │                       │                        │                       │
• Regex + LLM routing   │ • Tool registry       │ • Agent loop          │ • Unified task         │ • File tree index     │ • Workflow engine
• LocalAgent (Ollama)   │ • Tool executor       │   (plan→edit→validate)│   classifier           │ • Symbol index        │ • Milestone commands
• ClaudeAgent           │ • Safety gate         │ • Code editor         │ • Real safety/         │ • Deterministic       │ • State persistence
• Token tracking        │ • Migrated tools      │ • Diff renderer       │   approval enforcement │   context retriever   │ • Context curation
• REPL + single-shot    │ • search_code tool    │ • Agent memory        │ • Patch/precondition   │ • Optional semantic   │ • Context cache
• MCP integration       │ • write_file tool     │ • Streaming UX        │   edit model           │   search              │ • Claude structured I/O
                        │ • Schema validation   │ • Edit planning       │ • Run artifacts +      │ • Dependency hints    │ • Error recovery
                        │                       │ • Model routing/phase │   rollback correctness │ • Incremental updates │ • Progress display
                        │                       │                       │ • Local-first speed     │                       │ • Checkpoints
                        │                       │                       │   work                  │                       │
```

---

## Dependency Chain

```
v0.2 Tool Foundation
 └──► v0.3 Coding Agent (uses ToolExecutor from v0.2)
       └──► v0.3.5 Hardening + Performance (makes v0.3 safe, fast, and consistent)
             └──► v0.4 Codebase Intelligence (plugs deterministic retrieval into ANALYZE)
                   └──► v0.5 Workflow Automation (chains agent runs on top of trusted execution)
```

---

## Summary Table

| Version | Theme | New Files | Key Interfaces | External Deps |
|---|---|---|---|---|
| **v0.2** | Tool Foundation | 12 | `ToolDefinition`, `ToolRegistry`, `ToolExecutor`, `SafetyGate` | None |
| **v0.3** | Coding Agent | 14 | `CodingAgent`, `EditPlan`, `EditOperation`, `CodeEditor`, `AgentMemory`, `StreamEvent` | `diff` (npm) |
| **v0.3.5** | Hardening + Performance | 10 | `TaskClassifier`, `ApprovalPolicy`, `PatchOperation`, `RunArtifactStore`, `RetrievalFastPath` | None |
| **v0.4** | Codebase Intelligence | 18 | `FileIndex`, `SymbolIndex`, `ContextRetriever`, `DependencyGraph`, `BudgetManager` | `web-tree-sitter` |
| **v0.5** | Workflow Automation | 14 | `WorkflowEngine`, `WorkflowTemplate`, `StateManager`, `ContextCurator`, `ContextCache`, `CheckpointPolicy` | None |

---

## Config Evolution

Each version adds config sections. All are optional with sensible defaults.

| Version | New Config Sections |
|---|---|
| v0.2 | `safety` (always_confirm, auto_approve, allowed_write_paths) |
| v0.3 | `agent` (max_iterations, auto_confirm, validation_command), `editor` (show_diff, backup) |
| v0.3.5 | `runtime` (artifacts_dir, approval_mode, classifier), `performance` (cache, parallel_reads, warm_index) |
| v0.4 | `index` (languages, ignore patterns, storage_dir), `context_retrieval` (max_files, token budget, strategy) |
| v0.5 | `workflows` (storage_dir, custom_templates_dir, checkpoint_mode) |

---

## Detailed Plans

Each version has a dedicated design document:

- [v0.2 — Tool Foundation](2026-03-10-v02-tool-foundation.md)
- [v0.3 — Coding Agent](2026-03-10-v03-coding-agent.md)
- [v0.3.5 — Agent Hardening + Performance](2026-03-22-v035-agent-hardening-performance.md)
- [v0.4 — Codebase Intelligence](2026-03-10-v04-codebase-intelligence.md)
- [v0.5 — Workflow Automation](2026-03-10-v05-workflow-automation.md)

---

## Roadmap Notes

- **v0.3 is feature-complete but not execution-complete.** The next milestone focuses on trust: approvals, rollback correctness, artifact logging, and consistent routing.
- **v0.4 is retrieval-first, not embeddings-first.** File/symbol lookup and deterministic ranking ship before heavier semantic indexing.
- **v0.5 is assisted automation first.** Branch, push, and PR flows require explicit checkpoints until the execution core has proven reliability.

---

## Superseded Documents

The following earlier design docs are superseded by this roadmap:

- `2026-03-07-locode-v02-architecture-design.md` — combined v0.2-v0.5 into a single doc; now split into focused per-version plans
- `2026-03-10-local-coding-agent-architecture.md` — labeled as v0.4, now restructured as v0.3
